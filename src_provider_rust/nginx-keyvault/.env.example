# Azure Key Vault Configuration for nginx Keyless TLS
# Copy this file to .env and customize

# ═══════════════════════════════════════════════════════════════════
# Key Vault Configuration
# ═══════════════════════════════════════════════════════════════════

# Your Azure Key Vault name (without .vault.azure.net)
KEYVAULT_NAME=kv-kvtls-dev-001

# RSA key name for TLS (port 8443)
RSA_KEY_NAME=rsa-tls-key

# EC key name for TLS (port 8444)
EC_KEY_NAME=ec-tls-key

# ═══════════════════════════════════════════════════════════════════
# Azure Authentication
# ═══════════════════════════════════════════════════════════════════

# Tenant ID (required for Managed Identity)
AZURE_TENANT_ID=

# Client ID (for service principal or user-assigned managed identity)
# Leave empty for system-assigned managed identity or Azure CLI auth
AZURE_CLIENT_ID=

# Client Secret (for service principal auth only)
# Leave empty for managed identity or Azure CLI auth
AZURE_CLIENT_SECRET=

# ═══════════════════════════════════════════════════════════════════
# Provider Configuration
# ═══════════════════════════════════════════════════════════════════

# Path to the AKV provider library
AKV_PROVIDER_PATH=../target/release/akv_provider.so

# Log level (error, warn, info, debug, trace)
RUST_LOG=info

# ═══════════════════════════════════════════════════════════════════
# nginx Configuration
# ═══════════════════════════════════════════════════════════════════

# RSA TLS port
RSA_PORT=8443

# EC TLS port
EC_PORT=8444

# HTTP port (redirects to HTTPS)
HTTP_PORT=80
